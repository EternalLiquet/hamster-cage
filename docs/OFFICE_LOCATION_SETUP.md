# Office location setup contract (#77)

Issue #77 makes office creation usable without typing coordinates. The normal flow is **Name → Search address or Use my current location → review pin and radius → Save office**. The implementation has independent synthetic/emulator verification; final phone delivery and issue acceptance remain open. Manual coordinates and separate arrival walking grace (uncredited time before attendance starts) and exit/departure grace (projection only) remain under Advanced.

## Search, map and privacy

- Address search starts only when the user submits text. Android's [Geocoder](https://developer.android.com/reference/android/location/Geocoder) processes the typed query through the device's geocoding implementation, which may contact a network service. Search does not require location permission. Results are best effort: show enough identity to choose among matches, and never silently save the first result. A late result cannot replace a newer selection.
- Opening map review requests only the visible [OpenStreetMap raster tiles](https://operations.osmfoundation.org/policies/tiles/) over HTTPS. The tile service receives the viewed map area and ordinary network metadata. Show visible © OpenStreetMap contributors attribution, use a stable app-identifying User-Agent, honor cache headers (or retain tiles for at least seven days if headers cannot be interpreted), and avoid background prefetch or offline map downloads.
- **Use my current location** is an explicit, cancellable foreground one-shot request through the existing Play Services location dependency. Show progress, freshness and accuracy; denied or approximate permission, location off, timeout, stale fix and poor accuracy require an actionable retry, Settings or manual alternative. It must not require background permission or continuously upload fixes.
- Address text and viewed map area are the only office-setup data that may leave the app through these user actions. The app does not send attendance facts, policy or saved office history to either provider. Saving an office stores its center and radius locally; routine geofence capture, calculations, History and Settings continue without the lookup service or network.

When search or tiles are unavailable, keep existing offices and attendance usable. Offer clear retry, current location where available, and validated Advanced manual entry. Do not present a blank map as a confirmed center. Review and explicit Save are required before adding or replacing an office center; cancel/back preserves existing configuration and immutable attendance facts.

## Verified scope and remaining gates

For [PR #82](https://github.com/EternalLiquet/hamster-cage/pull/82), the [independent verifier recorded PASS](https://github.com/EternalLiquet/hamster-cage/pull/82#issuecomment-5819709371) at app head `d91a7fa7db8e50ce7e408aa996a6798dcfca6cf5`: API 37 OfficeScreen 9/9, map projection 4/4, tile/provider-label adversarial 2/2, and fresh/invalid one-shot fix 4/4. A live public address search led to explicit result selection, a full 150 m OpenStreetMap map review, TalkBack touch exploration and keyboard focus/Enter through labeled pin controls, and Save of a synthetic office at 360dp/200% text. With airplane mode on, Wi-Fi off and no default network, a fresh process reopened that office; a name-only edit retained the 150 m radius and Dashboard/History remained available without invented credit. Synthetic [screenshots](evidence/issue-77/README.md) accompany the check. All five CI checks succeeded at that exact head, including the API 35 offline journey. Spoken TalkBack audio and live stale/poor GPS injection were not captured; production-validator and fail-closed UI tests cover those fix values. A later PR head requires renewed exact-head checks before merge.

The final #35 candidate APK from source `0b12b2f6ea779d3ed39dc77d8f68c30226f8b692` was installed as a same-key update and cold-launched on a Samsung API36 phone; [artifact identity](BUILD_AND_INSTALL.md) and [phone receipt](https://github.com/EternalLiquet/hamster-cage/issues/35#issuecomment-5827146852) are recorded. The earlier hosted #77 artifact has a different development signer. Install/launch does not establish physical-phone office-flow or OS geofence delivery/recovery. #77, #16/#18–20/#33/#35 and epics #2/#6 retain their open criteria.
